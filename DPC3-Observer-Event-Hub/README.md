# DPC3 Observer Event Hub

Focus:

- `Observer`
- lifetime-safe subscriptions
- push versus pull event models

Students use `shared_ptr` and `weak_ptr` to keep an event system safe even
when listeners disappear before the publisher.
